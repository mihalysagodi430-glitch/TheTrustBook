<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>TheTrustBook — Master the Algorithm, Inspire the Crowd</title>
  <meta name="description" content="TheTrustBook — high-impact Instagram growth playbooks. Buy securely with PayPal." />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">

  <style>
    :root{
      --bg:#0b0d10; --bg2:#12161c; --card:#161b22; --ink:#eaf9ff;
      --mint:#00ffc6; --mint-2:#00e6b0; --rose:#ff6b6b; --cyan:#5cc8ff;
      --glow:#00bcd4; --shadow:0 12px 30px rgba(0,0,0,.45);
      --radius:22px;
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0; font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;
      color:var(--ink);
      background: radial-gradient(1200px 600px at 80% -20%, rgba(0,255,198,.08), transparent 60%),
                  radial-gradient(900px 500px at -20% 20%, rgba(92,200,255,.08), transparent 60%),
                  linear-gradient(180deg, #0b0d10, #0f1318 40%, #0b0d10 100%);
      -webkit-font-smoothing:antialiased; text-rendering:optimizeLegibility;
    }
    a{color:inherit}

    /* ===== Header with 3D-ish animated logo ===== */
    header{
      position:sticky; top:0; z-index:50;
      backdrop-filter:saturate(140%) blur(10px);
      background:linear-gradient(180deg, rgba(10,12,16,.9), rgba(10,12,16,.65));
      border-bottom:1px solid rgba(255,255,255,.06);
    }
    .wrap{max-width:1120px; margin:0 auto; padding:16px 20px; display:flex; gap:16px; align-items:center}
    .brand{display:flex; gap:14px; align-items:center; text-decoration:none}
    .brand h1{font-size:22px; margin:0; font-weight:800; letter-spacing:.2px; color:var(--mint)}
    .tag{opacity:.85; font-size:13px}

    /* inline SVG logo sizing */
    .logo{width:56px; height:56px; display:block; filter: drop-shadow(0 6px 18px rgba(0,255,198,.25))}
    .spin-saturn{transform-origin:50% 50%; animation:spin 12s linear infinite}
    .pulse-stars{animation:pulse 3s ease-in-out infinite alternate}
    @keyframes spin{to{transform:rotate(360deg)}}
    @keyframes pulse{from{opacity:.6} to{opacity:1}}

    /* ===== Hero ===== */
    .hero{padding:48px 20px 8px}
    .hero h2{
      margin:10px auto 0; max-width:820px; text-align:center; line-height:1.15;
      font-size:clamp(28px,5vw,44px); color:var(--mint)
    }
    .hero p{opacity:.86; text-align:center; margin:14px auto 0; max-width:760px}

    /* ===== Catalog grid ===== */
    .grid{
      max-width:1120px; margin:26px auto 32px; padding:0 20px;
      display:grid; grid-template-columns:repeat(2,minmax(0,1fr)); gap:22px;
    }
    @media (max-width:820px){ .grid{grid-template-columns:1fr} }

    .card{
      background:linear-gradient(180deg, rgba(255,255,255,.03), rgba(255,255,255,.01));
      border:1px solid rgba(255,255,255,.06);
      border-radius:var(--radius);
      box-shadow:var(--shadow);
      overflow:hidden; position:relative; isolation:isolate;
      transition:transform .28s ease, box-shadow .28s ease, border-color .28s;
    }
    .card::before{
      content:""; position:absolute; inset:-1px; border-radius:var(--radius);
      background:radial-gradient(600px 200px at 20% -20%, rgba(0,255,198,.08), transparent 60%),
                 radial-gradient(400px 140px at 120% 120%, rgba(92,200,255,.08), transparent 60%);
      z-index:0; pointer-events:none;
    }
    .card:hover{transform:translateY(-4px); border-color:rgba(0,255,198,.25); box-shadow:0 18px 50px rgba(0,0,0,.55)}

    .card-inner{display:grid; grid-template-rows:auto 1fr auto; position:relative; z-index:1; padding:22px}
    .card h3{margin:4px 0 8px; font-size:22px; color:var(--mint); letter-spacing:.2px}
    .hook{opacity:.9; font-style:italic; margin:0 0 10px}
    .desc{opacity:.88; margin:8px 0 14px}
    .price{font-weight:800; font-size:20px; color:var(--rose); margin-top:auto}

    .row{display:flex; gap:10px; flex-wrap:wrap}
    .btn{
      appearance:none; border:0; border-radius:14px; padding:12px 16px; font-weight:700;
      background:linear-gradient(90deg, var(--mint), var(--cyan)); color:#021312; cursor:pointer;
      transition:transform .15s ease, filter .15s ease; text-decoration:none; display:inline-flex; align-items:center; gap:8px;
      box-shadow:0 10px 24px rgba(0,255,198,.15);
    }
    .btn:hover{transform:translateY(-1px); filter:saturate(110%)}
    .ghost{background:transparent; color:var(--mint); border:1px solid rgba(0,255,198,.35)}

    /* ===== Section separators ===== */
    .section{max-width:1120px; margin:40px auto; padding:0 20px}
    .panel{
      background:var(--bg2); border:1px solid rgba(255,255,255,.06);
      border-radius:var(--radius); padding:22px; box-shadow:var(--shadow)
    }

    /* ===== Checkout modal ===== */
    .modal{position:fixed; inset:0; display:none; place-items:center; background:rgba(0,0,0,.5); z-index:100}
    .modal.open{display:grid}
    .sheet{
      width:min(560px,92vw); background:linear-gradient(180deg,#0e1217,#0b0f14);
      border:1px solid rgba(255,255,255,.08); border-radius:20px; padding:22px;
      box-shadow:0 30px 80px rgba(0,0,0,.6); animation:pop .18s ease;
    }
    @keyframes pop{from{transform:scale(.96); opacity:0} to{transform:scale(1); opacity:1}}
    .sheet h4{margin:0 0 6px; color:var(--mint)}
    .sheet .mini{opacity:.8; margin:0 0 16px}
    .close-x{position:absolute; top:14px; right:16px; background:transparent; border:0; color:#99f7e7; font-size:22px; cursor:pointer}

    /* ===== Footer ===== */
    footer{padding:28px 20px 60px; color:#cfeff7aa}
    footer a{color:#cfeff7}
    .legal{display:flex; gap:16px; flex-wrap:wrap; opacity:.9; font-size:14px}

    /* Smooth fade routing feel */
    .fadein{animation:fadein .5s ease both}
    @keyframes fadein{from{opacity:0; transform:translateY(6px)} to{opacity:1; transform:none}}
  </style>
</head>
<body>

  <!-- ===== Sticky Header with animated multi-universe logo ===== -->
  <header>
    <div class="wrap">
      <a class="brand" href="#top" aria-label="TheTrustBook home">
        <!-- Inline SVG logo (multi-universe: globe + ring + stars) -->
        <svg class="logo" viewBox="0 0 120 120" role="img" aria-label="TheTrustBook logo">
          <defs>
            <radialGradient id="g" cx="50%" cy="50%" r="60%">
              <stop offset="0%" stop-color="#00ffc6" stop-opacity="1"/>
              <stop offset="100%" stop-color="#00bcd4" stop-opacity=".7"/>
            </radialGradient>
          </defs>
          <!-- globe grid -->
          <g stroke="url(#g)" stroke-width="1.2" fill="none" opacity=".9">
            <circle cx="60" cy="60" r="34"/>
            <ellipse cx="60" cy="60" rx="34" ry="12"/>
            <ellipse cx="60" cy="60" rx="18" ry="34"/>
            <ellipse cx="60" cy="60" rx="8" ry="34"/>
            <ellipse cx="60" cy="60" rx="28" ry="34"/>
          </g>
          <!-- saturn ring -->
          <ellipse class="spin-saturn" cx="60" cy="60" rx="48" ry="16" fill="none" stroke="#00ffc6" stroke-opacity=".6" stroke-width="2"/>
          <!-- stars -->
          <g class="pulse-stars" fill="#00e6b0">
            <circle cx="20" cy="24" r="3"/><circle cx="96" cy="18" r="2.5"/>
            <circle cx="100" cy="100" r="3"/><circle cx="18" cy="96" r="2.5"/>
          </g>
        </svg>
        <div>
          <h1>TheTrustBook</h1>
          <div class="tag">Master the Algorithm • Inspire the Crowd</div>
        </div>
      </a>
    </div>
  </header>

  <!-- ===== Hero ===== -->
  <section class="hero fadein" id="top">
    <h2>Your fast lane to viral content, real growth, and conversion-ready audiences.</h2>
    <p>Pick a playbook, open the gates, and let the algorithm work for you. No fluff — just proven systems, templates, and action steps.</p>
  </section>

  <!-- ===== Catalog (2x2, right-to-left price pairing as requested) ===== -->
  <section class="grid fadein" id="catalog">
    <!-- Row 1: Starter 9.99 | Growth 15.99 -->
    <article class="card">
      <div class="card-inner">
        <h3>Starter Edition</h3>
        <p class="hook">“Get 10k Followers Fast with Us.”</p>
        <p class="desc">Quick wins, daily rituals, caption & hashtag cheatsheets — go from zero to momentum.</p>
        <div class="price">9.99 €</div>
        <div class="row">
          <button class="btn" data-sku="Starter Edition" data-price="9.99">Buy • PayPal</button>
          <a class="btn ghost" href="#contact">Questions?</a>
        </div>
      </div>
    </article>

    <article class="card">
      <div class="card-inner">
        <h3>Growth Edition</h3>
        <p class="hook">“Turn followers into a thriving community.”</p>
        <p class="desc">Engagement loops, analytics-driven timing, and retention mechanics that stick.</p>
        <div class="price">15.99 €</div>
        <div class="row">
          <button class="btn" data-sku="Growth Edition" data-price="15.99">Buy • PayPal</button>
          <a class="btn ghost" href="#contact">Questions?</a>
        </div>
      </div>
    </article>

    <!-- Row 2: Mastery 17.99 | Viral 12.99 -->
    <article class="card">
      <div class="card-inner">
        <h3>Mastery Edition</h3>
        <p class="hook">“Dominate Instagram like a pro!”</p>
        <p class="desc">Advanced systems, creator collaborations, and repeatable launch frameworks.</p>
        <div class="price">17.99 €</div>
        <div class="row">
          <button class="btn" data-sku="Mastery Edition" data-price="17.99">Buy • PayPal</button>
          <a class="btn ghost" href="#contact">Questions?</a>
        </div>
      </div>
    </article>

    <article class="card">
      <div class="card-inner">
        <h3>Viral Edition</h3>
        <p class="hook">“Catch the trend wave before it crashes.”</p>
        <p class="desc">Trend-mapping, sound-sniping, and reel recipes engineered for reach.</p>
        <div class="price">12.99 €</div>
        <div class="row">
          <button class="btn" data-sku="Viral Edition" data-price="12.99">Buy • PayPal</button>
          <a class="btn ghost" href="#contact">Questions?</a>
        </div>
      </div>
    </article>
  </section>

  <!-- ===== Embedded ebook preview (replace sample later if needed) ===== -->
  <div class="section fadein">
    <div class="panel">
      <h3 style="margin:0 0 10px; color:var(--mint)">Peek inside the playbook</h3>
      <p class="tag" style="margin:0 0 10px">Mobile-friendly PDF preview • Highly shareable visuals</p>
      <!-- Put your PDF into /assets/how-to-go-viral.pdf or change src below -->
      <iframe title="Ebook preview" src="about:blank"
              style="width:100%; height:420px; border:0; border-radius:14px; background:#0c1015 url('data:image/svg+xml;utf8,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 800 420%22><rect width=%22800%22 height=%22420%22 fill=%22%230b0d10%22/><text x=%22400%22 y=%22220%22 fill=%22%2300ffc6%22 font-family=%22Arial%22 font-size=%2230%22 text-anchor=%22middle%22>Upload your PDF to /assets and set iframe src</text></svg>') center/contain no-repeat;">
      </iframe>
    </div>
  </div>

  <!-- ===== Contact ===== -->
  <div id="contact" class="section fadein">
    <div class="panel">
      <h3 style="margin-top:0; color:var(--mint)">Contact</h3>
      <form onsubmit="sendMail(event)" class="row" style="flex-direction:column">
        <label>Name <input required id="name" placeholder="Your name"
                           style="width:100%; padding:12px 14px; border-radius:12px; border:1px solid rgba(255,255,255,.14); background:#0f1318; color:#eaf9ff;"></label>
        <label>Phone <input id="phone" placeholder="+36…"
                           style="width:100%; padding:12px 14px; border-radius:12px; border:1px solid rgba(255,255,255,.14); background:#0f1318; color:#eaf9ff; margin-top:10px"></label>
        <label>Email <input required id="email" type="email" placeholder="you@example.com"
                           style="width:100%; padding:12px 14px; border-radius:12px; border:1px solid rgba(255,255,255,.14); background:#0f1318; color:#eaf9ff; margin-top:10px"></label>
        <label>Message <textarea id="msg" rows="4" placeholder="How can we help?"
                           style="width:100%; padding:12px 14px; border-radius:12px; border:1px solid rgba(255,255,255,.14); background:#0f1318; color:#eaf9ff; margin-top:10px"></textarea></label>
        <div class="row" style="margin-top:12px">
          <button class="btn" type="submit">Send email</button>
          <a class="btn ghost" href="#top">Back to top</a>
        </div>
      </form>
    </div>
  </div>

  <!-- ===== Footer / Legal ===== -->
  <footer class="section fadein">
    <div class="panel">
      <div class="legal">
        <span>© <span id="y"></span> TheTrustBook. All rights reserved.</span>
        <a href="#" onclick="openLegal('terms');return false;">Terms</a>
        <a href="#" onclick="openLegal('privacy');return false;">Privacy</a>
        <a href="#" onclick="openLegal('license');return false;">License</a>
      </div>
    </div>
  </footer>

  <!-- ===== Checkout Modal (themed page, PayPal button inside) ===== -->
  <div class="modal" id="checkout">
    <div class="sheet">
      <button class="close-x" aria-label="Close" onclick="toggleCheckout(false)">✕</button>
      <h4 id="coTitle">Checkout</h4>
      <p class="mini">Secure payment via PayPal. Your PayPal details remain private.</p>
      <div class="panel" style="background:#0b0f14; margin-bottom:14px">
        <div class="row" style="justify-content:space-between; align-items:center">
          <div>
            <div id="coItem" style="font-weight:700">Item</div>
            <div class="tag" id="coMeta">Digital ebook (PDF)</div>
          </div>
          <div style="font-weight:800; color:var(--rose)" id="coPrice">€0.00</div>
        </div>
      </div>
      <div class="row" style="justify-content:flex-end">
        <button class="btn ghost" onclick="toggleCheckout(false)">Cancel</button>
        <a id="ppLink" class="btn" rel="noopener" target="_blank">Proceed to PayPal</a>
      </div>
    </div>
  </div>

  <!-- ===== Legal Modals ===== -->
  <div class="modal" id="legal">
    <div class="sheet" style="max-height:82vh; overflow:auto">
      <button class="close-x" aria-label="Close" onclick="toggleLegal(false)">✕</button>
      <h4 id="legalTitle">Legal</h4>
      <div id="legalBody" class="tag" style="white-space:pre-wrap; line-height:1.5"></div>
    </div>
  </div>

  <script>
    // Year
    document.getElementById('y').textContent = new Date().getFullYear();

    // Open themed checkout modal
    const checkout = document.getElementById('checkout');
    const ppBase = 'https://www.paypal.me/ThaTrust/';
    document.querySelectorAll('.btn[data-price]').forEach(b=>{
      b.addEventListener('click', ()=>{
        const sku = b.dataset.sku, price = b.dataset.price;
        document.getElementById('coTitle').textContent = 'Checkout — ' + sku;
        document.getElementById('coItem').textContent = sku;
        document.getElementById('coPrice').textContent = price + ' €';
        const link = ppBase + encodeURIComponent(price);
        const pp = document.getElementById('ppLink');
        pp.href = link;
        pp.textContent = 'Pay ' + price + ' € with PayPal';
        toggleCheckout(true);
      });
    });
    function toggleCheckout(show){
      checkout.classList.toggle('open', !!show);
    }

    // Contact (mailto, no server needed)
    function sendMail(e){
      e.preventDefault();
      const n = encodeURIComponent(document.getElementById('name').value.trim());
      const p = encodeURIComponent(document.getElementById('phone').value.trim());
      const em = encodeURIComponent(document.getElementById('email').value.trim());
      const m = encodeURIComponent(document.getElementById('msg').value.trim());
      const subject = encodeURIComponent('TheTrustBook Inquiry — ' + n);
      const body = encodeURIComponent(`Name: ${decodeURIComponent(n)}\nPhone: ${decodeURIComponent(p)}\nEmail: ${decodeURIComponent(em)}\n\nMessage:\n${decodeURIComponent(m)}`);
      window.location.href = `mailto:mihalysagodi430@gmail.com?subject=${subject}&body=${body}`;
    }

    // Legal text
    const LEGAL = {
      terms: `Terms of Service (Summary)

1. Digital Goods. All items sold are digital ebooks/playbooks. No physical shipment.
2. Delivery. After successful payment you receive download access via the email/contact you provide.
3. Refunds. Due to the nature of digital goods we generally cannot offer refunds once a file has been accessed.
4. Fair Use. You may use the content for your personal or business needs; reselling, republishing or sharing files publicly is prohibited.
5. Liability. We provide strategy, not guaranteed results. Use at your own discretion and always follow the platform's rules (Instagram, etc.).`,
      privacy: `Privacy Policy (Summary)

• We collect only information you submit (name, email, phone via the contact form) to respond to you and deliver your purchase.
• Payments are processed by PayPal. We do not store your card or PayPal credentials.
• We do not sell your data. For access, correction or deletion, contact us at mihalysagodi430@gmail.com.`,
      license: `License / Copyright

© ${new Date().getFullYear()} TheTrustBook. All rights reserved.

This website, brand assets and ebooks are protected works. You are granted a non-transferable license to use purchased files for your own projects. 
You may NOT redistribute, resell, repackage, sublicense, claim as your own, or create counterfeit versions of the site or its products.`
    };
    function openLegal(key){
      document.getElementById('legalTitle').textContent =
        key === 'terms' ? 'Terms & Conditions' : key === 'privacy' ? 'Privacy Policy' : 'License';
      document.getElementById('legalBody').textContent = LEGAL[key];
      toggleLegal(true);
    }
    const legal = document.getElementById('legal');
    function toggleLegal(show){ legal.classList.toggle('open', !!show); }

    // Close modals on background click
    [checkout, legal].forEach(modal=>{
      modal.addEventListener('click', (e)=>{
        if(e.target === modal){
          modal.classList.remove('open');
        }
      });
    });

    // Smooth anchor scroll
    document.querySelectorAll('a[href^="#"]').forEach(a=>{
      a.addEventListener('click', e=>{
        const id=a.getAttribute('href').slice(1);
        if(!id) return;
        const el=document.getElementById(id);
        if(el){ e.preventDefault(); el.scrollIntoView({behavior:'smooth', block:'start'}); }
      });
    });
  </script>
</body>
</html>
